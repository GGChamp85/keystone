# CLI reference

Every `keystone` command, from the CLI's own `--help` (generated by `scripts/gen_cli_reference.py`; CI fails when this page is stale). The CLI is a real HTTP client against a running deployment (`KEYSTONE_INFERENCE_URL`, `KEYSTONE_API_KEY`; admin commands need `KEYSTONE_ROOT_ADMIN_TOKEN`), except `init`, `doctor`, `up`/`down`, `bundle` and `deploy`, which work on the machine they run on.

## keystone

```text
Usage: root [OPTIONS] COMMAND [ARGS]...

 Keystone — self-hosted LLM inference & autonomous coding agents.

╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Commands ───────────────────────────────────────────────────────────────────────────────────────╮
│ keys-create  Create a real API key for a tenant — prints the key exactly once, like every other  │
│              real API-key flow.                                                                  │
│ keys-rotate  Replace an API key without an outage: mints a new key with the same scopes and      │
│              limits and expires the                                                              │
│              old one after --grace-hours (ADR 0004). Prints the new key exactly once.            │
│ up           Bring up the Keystone stack — a real wrapper around `docker compose up -d`          │
│              (+ optionally the sandbox image build and migrations), matching what the README's   │
│              manual `make certs && make build && make up && make db-migrate` steps do in one     │
│              command.                                                                            │
│ down         Stop the Keystone stack — a real wrapper around `docker compose down`.              │
│ ingest       Ingest a repository into the memory system's RAG index — a real wrapper around the  │
│              existing POST /v1/keystone/ingest route (src/memory/ingestion.py's                  │
│              CodeIngestionPipeline).                                                             │
│              Incremental: a second run only re-embeds files that actually changed.               │
│ init         Guided setup — writes a real .env file (from .env.example) for `make up` to use.    │
│              Run `keystone doctor` afterward to verify what's actually reachable.                │
│ doctor       Diagnose your Keystone deployment — real checks against Postgres (and its migration │
│              revision), Redis,                                                                   │
│              Qdrant, the sandbox daemon, model endpoints, the git host, package mirrors, secret  │
│              strength, TLS                                                                       │
│              certificate validity, free disk, and the running app's own readiness when           │
│              KEYSTONE_INFERENCE_URL is set —                                                     │
│              each reporting exactly what's wrong (and often how to fix it), not just pass/fail.  │
│ memory       Inspect and manage what the agent has learned.                                      │
│ finetune     Start and manage fine-tuning jobs.                                                  │
│ tenants      Bootstrap tenants (KEYSTONE_ROOT_ADMIN_TOKEN required).                             │
│ users        Manage tenant users (KEYSTONE_ROOT_ADMIN_TOKEN required).                           │
│ bundle       Build/import the air-gapped offline bundle (wraps airgap/*.sh).                     │
│ deploy       Deploy a model backend to a cloud (RunPod Serverless today).                        │
│ ide          Generate IDE configuration (VS Code via Continue).                                  │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone keys-create

```text
Usage: root keys-create [OPTIONS] {tenant_id}

 Create a real API key for a tenant — prints the key exactly once, like every other real API-key
 flow.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    tenant_id      <str>  [required]                                                            │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --name                   <str>  [default: default]                                               │
│ --scopes                 <str>  Comma-separated, e.g. inference,agent,finetune                   │
│                                 [default: inference,agent]                                       │
│ --expires-in-days        <int>                                                                   │
│ --help                          Show this message and exit.                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone keys-rotate

```text
Usage: root keys-rotate [OPTIONS] {tenant_id} {key_prefix}

 Replace an API key without an outage: mints a new key with the same scopes and limits and expires
 the old one after --grace-hours (ADR 0004). Prints the new key exactly once.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    tenant_id       <str>  [required]                                                           │
│ *    key_prefix      <str>  The ks-xxxxxxxx prefix of the key to replace [required]              │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --grace-hours        <int>  How long the old key keeps working; 0 = revoke now [default: 24]     │
│ --help                      Show this message and exit.                                          │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone up

```text
Usage: root up [OPTIONS]

 Bring up the Keystone stack — a real wrapper around `docker compose up -d` (+ optionally the
 sandbox image build and migrations), matching what the README's manual `make certs && make build
 && make up && make db-migrate` steps do in one command.

╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --dev                --no-dev                 Only infra (postgres/redis/qdrant) — no            │
│                                               app/worker/sandbox-daemon                          │
│                                               [default: no-dev]                                  │
│ --migrate            --no-migrate             Run alembic migrations once the stack is up        │
│                                               [default: migrate]                                 │
│ --sandbox-images     --no-sandbox-images      Build the sandbox runtime image too                │
│                                               [default: sandbox-images]                          │
│ --with-demo-model                             Also start the no-GPU demo model (llama.cpp,       │
│                                               Qwen2.5-Coder-0.5B) — pair with `keystone init     │
│                                               --backend demo-cpu`                                │
│ --help                                        Show this message and exit.                        │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone down

```text
Usage: root down [OPTIONS]

 Stop the Keystone stack — a real wrapper around `docker compose down`.

╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone ingest

```text
Usage: root ingest [OPTIONS] {repository_url}

 Ingest a repository into the memory system's RAG index — a real wrapper around the existing POST
 /v1/keystone/ingest route (src/memory/ingestion.py's CodeIngestionPipeline). Incremental: a second
 run only re-embeds files that actually changed.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    repository_url      <str>  Git clone URL — must be on GIT_ALLOWED_HOSTS [required]          │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --branch                  <str>  [default: main]                                                 │
│ --max-file-size-kb        <int>  Skip files larger than this [default: 500]                      │
│ --help                           Show this message and exit.                                     │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone init

```text
Usage: root init [OPTIONS]

 Guided setup — writes a real .env file (from .env.example) for `make up` to use. Run `keystone
 doctor` afterward to verify what's actually reachable.

╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --output         <str>  Where to write the .env file [default: .env]                             │
│ --yes                   Non-interactive: auto-generate all secrets, skip every other prompt      │
│ --backend        <str>  Coding model backend: demo-cpu (llama.cpp 0.5B, no GPU), single-gpu      │
│                         (vLLM serving Qwen2.5-Coder-7B on one 24 GB GPU), frontier-proxy         │
│                         (benchmarks/frontier_proxy.py), or self-hosted-gpu (the compose/Helm     │
│                         defaults). Asked interactively when omitted.                             │
│ --help                  Show this message and exit.                                              │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone doctor

```text
Usage: root doctor [OPTIONS]

 Diagnose your Keystone deployment — real checks against Postgres (and its migration revision),
 Redis, Qdrant, the sandbox daemon, model endpoints, the git host, package mirrors, secret
 strength, TLS certificate validity, free disk, and the running app's own readiness when
 KEYSTONE_INFERENCE_URL is set — each reporting exactly what's wrong (and often how to fix it), not
 just pass/fail.

╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone memory

```text
Usage: root memory [OPTIONS] COMMAND [ARGS]...

 Inspect and manage what the agent has learned.

╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Commands ───────────────────────────────────────────────────────────────────────────────────────╮
│ list     List memories visible to your tenant.                                                   │
│ add      Add a new memory.                                                                       │
│ recall   Preview exactly what the agent would recall for a given query — the real ranking, not   │
│          an approximation.                                                                       │
│ approve  Approve a proposed memory so it becomes recallable.                                     │
│ forget   Forget a memory (soft delete — it stops being recalled, but the record and its          │
│          attribution stay).                                                                      │
│ pin      Pin a memory — it's always recalled regardless of query relevance.                      │
│ unpin    Unpin a memory.                                                                         │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone memory list

```text
Usage: root memory list [OPTIONS]

 List memories visible to your tenant.

╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --repository        <str>  Filter to a specific repo's memories                                  │
│ --scope             <str>  repo | tenant                                                         │
│ --status            <str>  proposed | approved | forgotten                                       │
│ --help                     Show this message and exit.                                           │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone memory add

```text
Usage: root memory add [OPTIONS] {content}

 Add a new memory.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    content      <str>  The memory text [required]                                              │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --kind                         <str>  convention | preference | fact | avoid [default: fact]     │
│ --repository                   <str>  Repo clone URL — omit for a tenant-wide memory             │
│ --pinned        --no-pinned           Always include this memory regardless of query relevance   │
│                                       [default: no-pinned]                                       │
│ --help                                Show this message and exit.                                │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone memory recall

```text
Usage: root memory recall [OPTIONS] {query}

 Preview exactly what the agent would recall for a given query — the real ranking, not an
 approximation.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    query      <str>  The task/question to preview recall for [required]                        │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --repository           <str>  Repo clone URL for repo-scope recall                               │
│ --budget-tokens        <int>  Token budget, matching the agent's own [default: 2000]             │
│ --help                        Show this message and exit.                                        │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone memory approve

```text
Usage: root memory approve [OPTIONS] {memory_id}

 Approve a proposed memory so it becomes recallable.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    memory_id      <str>  [required]                                                            │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone memory forget

```text
Usage: root memory forget [OPTIONS] {memory_id}

 Forget a memory (soft delete — it stops being recalled, but the record and its attribution stay).

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    memory_id      <str>  [required]                                                            │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone memory pin

```text
Usage: root memory pin [OPTIONS] {memory_id}

 Pin a memory — it's always recalled regardless of query relevance.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    memory_id      <str>  [required]                                                            │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone memory unpin

```text
Usage: root memory unpin [OPTIONS] {memory_id}

 Unpin a memory.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    memory_id      <str>  [required]                                                            │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone finetune

```text
Usage: root finetune [OPTIONS] COMMAND [ARGS]...

 Start and manage fine-tuning jobs.

╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Commands ───────────────────────────────────────────────────────────────────────────────────────╮
│ start     Start a fine-tuning job.                                                               │
│ guided    Describe → plan & cost → approve → train. Builds a dataset from the repositories' real │
│           history and                                                                            │
│           this tenant's accepted agent tasks, shows the plan, and starts the job once you        │
│           approve it.                                                                            │
│ approve   Approve a planned guided fine-tune and start it.                                       │
│ list      List fine-tuning jobs for your tenant.                                                 │
│ status    Show one job's current status and metrics.                                             │
│ preview   Preview the first records of a job's real training data.                               │
│ promote   Promote a completed job's adapter to the tenant's default for its base model.          │
│ rollback  Retire a promoted job's adapter — routing falls back to the base model.                │
│ watch     Stream a job's live progress until it reaches a terminal status.                       │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone finetune start

```text
Usage: root finetune start [OPTIONS] {job_type} {base_model}
                            {training_data_path}

 Start a fine-tuning job.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    job_type                <str>  lora | sft | dpo [required]                                  │
│ *    base_model              <str>  Base model id, e.g. Qwen/Qwen2.5-Coder-32B-Instruct          │
│                                     [required]                                                   │
│ *    training_data_path      <str>  Path to the training_data JSONL, reachable by the server     │
│                                     [required]                                                   │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --set         <str>  Trainer config override KEY=VALUE, repeatable (e.g. --set num_epochs=3)     │
│ --help               Show this message and exit.                                                 │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone finetune guided

```text
Usage: root finetune guided [OPTIONS]

 Describe → plan & cost → approve → train. Builds a dataset from the repositories' real history and
 this tenant's accepted agent tasks, shows the plan, and starts the job once you approve it.

╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ *  --repo                               <str>                       Allow-listed git URL to      │
│                                                                     learn from (repeatable)      │
│                                                                     [required]                   │
│ *  --goal                               <str>                       What the adapter should get  │
│                                                                     better at, in plain words    │
│                                                                     [required]                   │
│    --base-model                         <str>                       Catalog model id, or 'auto'  │
│                                                                     for the default SLM          │
│                                                                     [default: auto]              │
│    --epochs                             <int range> [x>=1]          [default: 1]                 │
│    --holdout-ratio                      <float range>               [default: 0.2]               │
│                                         [0.05<=x<=0.5]                                           │
│    --gpu                                <str>                       Target GPU as NAME:VRAM_GB   │
│                                                                     (repeatable); omitted =      │
│                                                                     detect on the server         │
│    --gpu-hourly-cost                    <float>                     Your GPU price in USD/hour,  │
│                                                                     for the cost line            │
│    --yes              -y                                            Approve the plan without     │
│                                                                     asking                       │
│    --force                --no-force                                Approve even if the planner  │
│                                                                     says it does not fit         │
│                                                                     [default: no-force]          │
│    --help                                                           Show this message and exit.  │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone finetune approve

```text
Usage: root finetune approve [OPTIONS] {job_id}

 Approve a planned guided fine-tune and start it.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    job_id      <str>  [required]                                                               │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --force    --no-force      Approve even if the planner says it does not fit [default: no-force]  │
│ --help                     Show this message and exit.                                           │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone finetune list

```text
Usage: root finetune list [OPTIONS]

 List fine-tuning jobs for your tenant.

╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --status        <str>  Filter by status                                                          │
│ --help                 Show this message and exit.                                               │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone finetune status

```text
Usage: root finetune status [OPTIONS] {job_id}

 Show one job's current status and metrics.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    job_id      <str>  [required]                                                               │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone finetune preview

```text
Usage: root finetune preview [OPTIONS] {job_id}

 Preview the first records of a job's real training data.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    job_id      <str>  [required]                                                               │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --lines        <int>  Number of records to preview [default: 5]                                  │
│ --help                Show this message and exit.                                                │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone finetune promote

```text
Usage: root finetune promote [OPTIONS] {job_id}

 Promote a completed job's adapter to the tenant's default for its base model.

 Gated by the verdict: the adapter must have beaten the base model on the
 held-out split. The promoted adapter is written to the lora_modules
 manifest and live-loaded into the serving vLLM when it allows it.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    job_id      <str>  [required]                                                               │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --force          Admin only: promote despite a fail/unknown verdict (audit-logged)               │
│ --help           Show this message and exit.                                                     │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone finetune rollback

```text
Usage: root finetune rollback [OPTIONS] {job_id}

 Retire a promoted job's adapter — routing falls back to the base model.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    job_id      <str>  [required]                                                               │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone finetune watch

```text
Usage: root finetune watch [OPTIONS] {job_id}

 Stream a job's live progress until it reaches a terminal status.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    job_id      <str>  [required]                                                               │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone tenants

```text
Usage: root tenants [OPTIONS] COMMAND [ARGS]...

 Bootstrap tenants (KEYSTONE_ROOT_ADMIN_TOKEN required).

╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Commands ───────────────────────────────────────────────────────────────────────────────────────╮
│ create      Create a new tenant — the real bootstrap step every API key/user belongs to.         │
│ list        List every tenant on this deployment.                                                │
│ set-limits  Set a tenant's caps — a monthly dollar budget, token budgets, concurrency. Only the  │
│             options given change.                                                                │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone tenants create

```text
Usage: root tenants create [OPTIONS] {name} {email}

 Create a new tenant — the real bootstrap step every API key/user belongs to.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    name       <str>  [required]                                                                │
│ *    email      <str>  [required]                                                                │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --tier        <str>  free | pro | enterprise [default: free]                                     │
│ --help               Show this message and exit.                                                 │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone tenants list

```text
Usage: root tenants list [OPTIONS]

 List every tenant on this deployment.

╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone tenants set-limits

```text
Usage: root tenants set-limits [OPTIONS] {tenant_id}

 Set a tenant's caps — a monthly dollar budget, token budgets, concurrency. Only the options given
 change.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    tenant_id      <str>  [required]                                                            │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --monthly-budget-usd           <float>  USD per calendar month; 0 = no budget                    │
│ --daily-token-limit            <int>    tokens/day; 0 = unlimited                                │
│ --monthly-token-limit          <int>    tokens/month; 0 = unlimited                              │
│ --max-concurrent-agents        <int>    running tasks; 0 = no cap                                │
│ --help                                  Show this message and exit.                              │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone users

```text
Usage: root users [OPTIONS] COMMAND [ARGS]...

 Manage tenant users (KEYSTONE_ROOT_ADMIN_TOKEN required).

╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Commands ───────────────────────────────────────────────────────────────────────────────────────╮
│ add   Add a user to a tenant — real identity attribution on every task/memory action they take.  │
│ list  List every user on a tenant.                                                               │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone users add

```text
Usage: root users add [OPTIONS] {tenant_id} {name} {email}

 Add a user to a tenant — real identity attribution on every task/memory action they take.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    tenant_id      <str>  [required]                                                            │
│ *    name           <str>  [required]                                                            │
│ *    email          <str>  [required]                                                            │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --role        <str>  admin | lead | developer [default: developer]                               │
│ --help               Show this message and exit.                                                 │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone users list

```text
Usage: root users list [OPTIONS] {tenant_id}

 List every user on a tenant.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│ *    tenant_id      <str>  [required]                                                            │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone bundle

```text
Usage: root bundle [OPTIONS] COMMAND [ARGS]...

 Build/import the air-gapped offline bundle (wraps airgap/*.sh).

╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Commands ───────────────────────────────────────────────────────────────────────────────────────╮
│ download-models  Download every model weight onto this (internet-connected) build machine —      │
│                  wraps airgap/download_models.sh.                                                │
│ build-images     Pull and tarball every container image onto this build machine — wraps          │
│                  airgap/build_image_bundle.sh.                                                   │
│ import           Load the transferred bundle on the air-gapped target — wraps                    │
│                  airgap/import_bundle.sh.                                                        │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone bundle download-models

```text
Usage: root bundle download-models [OPTIONS] [output_dir]

 Download every model weight onto this (internet-connected) build machine — wraps
 airgap/download_models.sh.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│   output_dir      <str>  [default: ./airgap/models]                                              │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone bundle build-images

```text
Usage: root bundle build-images [OPTIONS] [output_dir]

 Pull and tarball every container image onto this build machine — wraps
 airgap/build_image_bundle.sh.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│   output_dir      <str>  [default: ./airgap/output/images]                                       │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone bundle import

```text
Usage: root bundle import [OPTIONS] [bundle_dir]

 Load the transferred bundle on the air-gapped target — wraps airgap/import_bundle.sh.

╭─ Arguments ──────────────────────────────────────────────────────────────────────────────────────╮
│   bundle_dir      <str>                                                                          │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --push    --no-push      Also push each re-tagged image to the internal registry                 │
│                          [default: no-push]                                                      │
│ --help                   Show this message and exit.                                             │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone deploy

```text
Usage: root deploy [OPTIONS] COMMAND [ARGS]...

 Deploy a model backend to a cloud (RunPod Serverless today).

╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Commands ───────────────────────────────────────────────────────────────────────────────────────╮
│ runpod-serverless  Create (or reuse) a RunPod Serverless vLLM endpoint serving an open-weight    │
│                    model,                                                                        │
│                    scaled to zero when idle — a real GPU backend with no hardware to buy. Needs  │
│                    RUNPOD_API_KEY in .env. Prints the exact .env lines that point a role at it.  │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone deploy runpod-serverless

```text
Usage: root deploy runpod-serverless [OPTIONS]

 Create (or reuse) a RunPod Serverless vLLM endpoint serving an open-weight model, scaled to zero
 when idle — a real GPU backend with no hardware to buy. Needs RUNPOD_API_KEY in .env. Prints the
 exact .env lines that point a role at it.

╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --name                             <str>  Endpoint/template name — re-running with the same name │
│                                           reuses them                                            │
│                                           [default: keystone-coder]                              │
│ --model                            <str>  Hugging Face model id to serve                         │
│                                           [default: Qwen/Qwen2.5-Coder-7B-Instruct]              │
│ --served-name                      <str>  The `model` name clients send                          │
│                                           [default: keystone-coder]                              │
│ --gpu-tier                         <str>  24gb | 80gb — which serverless GPU pool to schedule on │
│                                           [default: 24gb]                                        │
│ --max-model-len                    <int>  [default: 16384]                                       │
│ --workers-max                      <int>  Upper bound on concurrent GPU workers (cost ceiling)   │
│                                           [default: 2]                                           │
│ --idle-timeout                     <int>  Seconds idle before a worker scales to zero            │
│                                           [default: 60]                                          │
│ --destroy          --no-destroy           Delete the endpoint and template of this name instead  │
│                                           [default: no-destroy]                                  │
│ --help                                    Show this message and exit.                            │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone ide

```text
Usage: root ide [OPTIONS] COMMAND [ARGS]...

 Generate IDE configuration (VS Code via Continue).

╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ --help          Show this message and exit.                                                      │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
╭─ Commands ───────────────────────────────────────────────────────────────────────────────────────╮
│ continue-config  Render Continue's config.yaml (VS Code) pointing chat/edit/apply, autocomplete  │
│                  and MCP memory at                                                               │
│                  this Keystone deployment — the same gateway and key OpenCode uses. See          │
│                  docs/guides/use-from-vscode.md.                                                 │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```

## keystone ide continue-config

```text
Usage: root ide continue-config [OPTIONS]

 Render Continue's config.yaml (VS Code) pointing chat/edit/apply, autocomplete and MCP memory at
 this Keystone deployment — the same gateway and key OpenCode uses. See
 docs/guides/use-from-vscode.md.

╭─ Options ────────────────────────────────────────────────────────────────────────────────────────╮
│ *  --base-url                  <str>   Keystone gateway URL, no trailing /v1 (e.g.               │
│                                        https://keystone.internal:8080)                           │
│                                        [required]                                                │
│ *  --api-key                   <str>   A Keystone API key with the inference and agent scopes    │
│                                        [env var: KEYSTONE_API_KEY]                               │
│                                        [required]                                                │
│    --output                    <path>  Write here (typically ~/.continue/config.yaml); prints to │
│                                        stdout when omitted                                       │
│    --ca-bundle                 <path>  Internal CA bundle (pki/) for an air-gapped TLS           │
│                                        deployment                                                │
│    --coding-model              <str>   Model name for chat/edit/apply [default: coding]          │
│    --autocomplete-model        <str>   Model name for autocomplete [default: coding_fallback]    │
│    --help                              Show this message and exit.                               │
╰──────────────────────────────────────────────────────────────────────────────────────────────────╯
```
