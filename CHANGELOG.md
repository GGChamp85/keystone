# Changelog

All notable changes to this project are documented here. Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/); versioning follows [Semantic Versioning](https://semver.org/).

## [Unreleased]

Platform foundation — self-hosted, air-gapped LLM inference gateway and autonomous coding agent platform:

### Added
- **Keystone Inference**: OpenAI-compatible gateway (`/v1/chat/completions`, `/v1/models`), API key management, rate limiting, token budgets, subscription tiers, multi-model routing (coding/coding_fallback/reasoning) with health-aware fallback and per-tenant adapter resolution.
- **Keystone Agents**: LangGraph Plan→Code→Quality→Review→Test→Fix background agent with real tool use (read/grep/run/patch, not full-file rewrites), a real git workflow (clone → branch → commit → push → PR against a real Gitea), Temporal-backed durable execution, and a live plan/execution-trace web UI (`web/`, React SPA served at `/app`) streaming progress over Server-Sent Events.
- **Multi-developer + scale**: user/role identity (`admin`/`lead`/`developer`) on every task and memory action; a Redis-backed per-tenant/per-user concurrency limiter; automatic PR-status polling into `task_feedback`; incremental, AST-aware, per-tenant repo indexing for large repos/monorepos.
- **Memory system**: a per-repo/per-tenant memory the agent learns from and a human can inspect, pin, approve, and forget — exposed via `keystone memory ...`, the web UI's Memory panel, and an OpenCode plugin/MCP server.
- **Fine-tuning, end to end**: real training-data sources (git history, accepted-task trajectories, repo pretraining text), a stratified train/holdout manifest with real content hashes, eval-aware LoRA/SFT/DPO trainers, an adapter registry with promote/rollback, LoRA-serving vLLM flags, adapter-aware routing through every real inference call site, a Temporal-backed job runner with an asyncio fallback, a full `/v1/finetune` REST API, `keystone finetune ...` CLI commands, and a 3-step web wizard (pick data → start → verdict).
- **Distributed model serving**: per-role horizontal replica scaling behind a load-balanced Service, real multi-node pipeline-parallel serving via vLLM's Ray executor for models too large for one GPU node, and KEDA-based autoscaling on the real `vllm:num_requests_waiting` metric — all three paths rendered and verified in CI via `helm lint`/`helm template`.
- **Real, non-mocked benchmark harness** (`benchmarks/`): scores a model's completion by actually executing it in a real sandbox (gVisor) — no LLM-as-judge — with a real token-cost comparison against gated frontier clients (Anthropic/OpenAI, only constructed when a real API key is present). Run for real against `claude-opus`/`claude-sonnet` in this repo's own development environment; see the README's benchmark section for the actual numbers.
- **Real repo-task agent benchmark** (`benchmarks/agent_runner.py`): the actual agent loop — `src/orchestrator/engine.py`'s real engine, real tools, real quality gates — run against a seeded real Gitea repo with a real bug and a real failing test, end to end. `benchmarks/frontier_proxy.py` (new) makes a real frontier model usable as a coding-role backend by translating `src/inference/client.py`'s exact wire protocol to and from the real Anthropic Messages API — no changes needed to `InferenceClient` or any orchestrator node. Run for real: the `token_bucket` task was solved by the frontier model in 67.6s across 5 real iterations (plan → tool-based fix → quality gates → review → tests → push → PR), independently confirmed by cloning the pushed branch in a fresh sandbox and re-running both held-out tests.
- **Self-hosted sandboxing**: Firecracker microVMs (primary) with an automatic gVisor fallback where KVM isn't available; host-side-only network egress enforcement.
- **Air-gap packaging**: offline bundle scripts for container images, Python wheels, npm packages, and model weights; a real internal CA (`pki/`); verified zero-egress runtime for the core stateful stack.
- **Observability**: Prometheus, Grafana (3 real dashboards), Loki, Promtail, and Alertmanager with 6 real alert rules — deployed and verified on both docker-compose and a live Kubernetes cluster.
- **Infrastructure**: Kubernetes/Helm chart (client-VPC production tier) and OpenTofu modules (RunPod dev/test tier); NetworkPolicy-segmented tiers; OpenBao-backed secrets with a real init/unseal/seed lifecycle.
- **Compliance**: full third-party license inventory (`docs/LICENSES_AND_COMPLIANCE.md`) and telemetry audit (`docs/TELEMETRY_AUDIT.md`), including catching and fixing a Redis relicensing issue (switched to Valkey) before it shipped.
- **`keystone doctor`**: real diagnostics against Postgres, Redis, Qdrant, the sandbox daemon, every model endpoint, the git host, package mirrors, and secret strength — connects to configured infrastructure directly, so it works even before the app itself is up, and reports exactly what's wrong rather than a bare pass/fail.
- **`keystone init`**: guided setup that writes a real, working `.env` from `.env.example` with strong random secrets generated by default, and can interactively configure a git host and pick between a self-hosted GPU backend or `benchmarks/frontier_proxy.py`.
- **`keystone tenants create`/`keystone users add`/`list`/`keystone keys-create`**: real CLI wrappers around the existing root-admin-token-gated `/v1/admin` routes — the first CLI-based bootstrap path for tenants/users/API keys this project has had.
- **`keystone up`/`down`/`bundle download-models|build-images|import`**: real subprocess wrappers (stdout/stderr inherited, real exit codes propagated) around `docker compose` and the existing `airgap/*.sh` scripts — verified for real, not just that the constructed command looked plausible.
- **`keystone ingest`**: a real CLI wrapper around the existing `POST /v1/keystone/ingest` route (`src/memory/ingestion.py`'s `CodeIngestionPipeline`) — repo ingestion no longer needs to be triggered indirectly through a coding task. Run for real against a live server and a real public GitHub repo: the first run processed 23 files into 61 chunks; a second run against the same commit correctly reported all 23 files unchanged and 0 chunks re-created, proving the incremental skip path.
- **`benchmarks/eval_prompt.py`**: an ad hoc single-input eval — run one arbitrary prompt (optionally with a real test) through Keystone Inference and any configured frontier models side by side, without writing a `benchmarks/tasks/*.json` file first. Reuses the same real sandbox scoring path and cost model as the fixed task suite. Run for real: both unscored (raw completions) and scored (a real ad hoc test, real sandbox pass/fail) modes verified end to end against a live sandbox and a live Anthropic API.
- **PII redaction at the frontier-model boundary** (`src/security/pii_redaction.py`): real, deterministic detection of email/phone/SSN/credit-card (real Luhn checksum)/IPv4 patterns, wired into `benchmarks/frontier_proxy.py` behind an explicit opt-in (`FRONTIER_PROXY_REDACT_PII=1`) — the one real point where request content leaves the self-hosted network for a third-party API. Verified for real against a live request: with the flag on, a real email/phone were confirmed redacted before reaching the frontier model (via the proxy's own logs); with it off, the same real value round-tripped through the model unchanged.
- **Task-complexity-aware model routing**: `AgentTaskRequest.model` accepts `"auto"`, resolved once by `src/inference/model_router.py`'s real heuristics (`classify_task_to_role` for reasoning vs. coding, and — opt-in via `TASK_COMPLEXITY_ROUTING_ENABLED=1` — `classify_task_complexity` for simple vs. complex within coding) before the task is persisted, so the resolved role is always visible in `AgentTask.model_role`, never a hidden live decision. Deliberately conservative: an ambiguous or unmatched task defaults toward the more capable model, not the cheaper one. Verified for real: submitting a task with `model="auto"` against a live Postgres/Redis correctly persisted `reasoning` for a review-flavored description and `coding_fallback` for a "fix a typo" description with the flag enabled.
- **Prompt-injection defense on every tool result** (`src/security/prompt_injection.py`): every successful tool result the coding agent reads — file contents, grep matches, command output — is wrapped in an explicit `<tool_output tool=... detail=...>` untrusted-data delimiter naming its real source (spotlighting) before it re-enters the model's context, and real regex detection for common injection phrasings ("ignore previous instructions", role overrides, system-prompt exfiltration, named jailbreak personas) adds a visible security notice inside that specific result on a match. Content is never altered or dropped, so unlike PII redaction this is on by default. The detail attribute is HTML-escaped and length-capped so a quote inside a real shell command can't forge tag syntax. Defense in depth, honestly scoped: a disguised injection can still evade the patterns.
- **A real, no-GPU model backend for CI and laptops** (`demo-model` compose profile, `make up-demo`): llama.cpp's server pulling the official Apache-2.0 Qwen2.5-Coder-0.5B-Instruct GGUF (676 MB, cached after the first run) — a real OpenAI-compatible endpoint with `/v1/models`, tool calling (`--jinja`), streaming with usage, and JSON-schema output, so every model-dependent test can run for real instead of self-skipping. CI now starts it for the whole test job and caches the GGUF across runs. `tests/e2e/test_ci_backend.py` verifies health, a real completion with real token usage, and real streaming against it. Probed for real through the same client: JSON-schema structured output (`chat_structured`) returns schema-conformant JSON on this model; native `tool_calls` do not come back reliably — it emits the call as a fenced JSON block in the message content instead, which is the shape the existing text tool protocol (`src/orchestrator/tools/protocol.py`) parses, so that is the protocol the demo role uses for agent-loop tests. What it does not claim: a 0.5B model's answer quality — that comes from the GPU tiers and the benchmark suite.
- **A real trainer run in CI** (`docker/training.Dockerfile`, `tests/test_trainer_smoke.py`, CI job `train-smoke`): a Python 3.12 training image with CPU torch and the `finetuning` extra — pinned to 3.12 on purpose, since the ML stack lags the newest CPython and the main app's own venv runs an interpreter where these wheels aren't installable — and a test that trains a real LoRA adapter on the real Qwen2.5-Coder-0.5B-Instruct on CPU (two optimizer steps, a real held-out `eval_loss`), saves it, and reloads it with PEFT for continued training. The first trainer execution this repository has ever had; it immediately caught two real trainer bugs (see Fixed). A smoke, not a training claim.
- **`LoRATrainingConfig.max_steps`**: a hard optimizer-step budget that overrides `num_epochs` — what a bounded smoke run and a cost-estimated guided run both need.
- **Per-role bearer tokens** (`VLLM_*_API_KEY`) so a hosted OpenAI-compatible endpoint such as RunPod Serverless can serve a role, plus `RUNPOD_API_KEY`/`RUNPOD_ENDPOINT_URL` settings and a `keystone doctor` check that distinguishes unconfigured, half-configured, and unreachable from a real authenticated round trip. The fake vLLM test fixture records request headers so the token is proven to reach the server.
- **`keystone deploy runpod-serverless`** (`src/cli/runpod_serverless.py`): a real client for RunPod's REST API that stands up a serverless vLLM endpoint (RunPod's maintained `worker-v1-vllm` image) serving an open-weight model — default Qwen2.5-Coder-7B-Instruct on the 24GB tier — scaled to zero when idle, so a GPU backend costs $0 until the first request. Idempotent by name (re-running reuses the existing template/endpoint rather than creating billable duplicates); `--destroy` tears both down. Request shapes are asserted against RunPod's published OpenAPI document, not guessed. REST rather than OpenTofu because the provider ships no template resource and an endpoint requires one. Run for real: the template was created; endpoint creation was refused by RunPod with an HTTP 500 whose body says the account has $0 credit — now translated into a plain instruction (add credit, re-run) instead of a raw error, with that exact real response body as the test fixture. `docs/deployment/RUNPOD_SETUP.md` now leads with this path.
- **Repository map for the coding agent** (`src/orchestrator/repo_map.py`, tool `get_repo_map`): the planner and the coding loop now start from a ranked outline of the whole repository — every class/function/method/type with file, line and signature, most-referenced code first, within a token budget — instead of a blind `find -maxdepth 2`. Built inside the task's real sandbox by universal-ctags (one apt package in the sandbox image, every language the agent meets; not tree-sitter, whose grammar wheels would have to ride in the air-gap bundle — ADR 0001) plus one `grep -F` pass that counts how often each symbol name is used, so a file whose symbols are called from everywhere ranks above a leaf. The map is built once per task right after the clone (the planning node now clones, so the plan names real files) and cached on the state; if ctags is missing from a custom sandbox image the map degrades to a labelled file list rather than silently disappearing. Verified for real: ctags ran inside a live sandbox and produced the expected symbols, signatures, line numbers and reference counts (`tests/test_repo_map.py`).
- **`read_file` line ranges** (`start_line`/`end_line`, numbered output) so the agent reads the region the map pointed at instead of whole files, and **fuzzy `apply_patch`**: a `search` block that differs from the file only in whitespace/indentation, or is ≥92% similar, is applied when — and only when — it matches exactly one place; the result says a fuzzy match was used and where. Two candidate places are refused as ambiguous, never guessed (`tests/test_fuzzy_patch.py`, real-sandbox cases in `tests/test_tool_impl.py`).
- **The agent installs the repo's dependencies and verifies its own work**: `repo_profile.install_cmd` was detected but never executed — the repo's real test suite ran against a bare sandbox. It now runs once per task right after the clone (`nodes/_shared.py::install_dependencies`, pointed at the configured `PIP_INDEX_URL`/`NPM_CONFIG_REGISTRY`/`GOPROXY` mirrors, fail-fast retries), and a failed install is recorded on the state and shown to the coding model so a `ModuleNotFoundError` is attributed to the install rather than mistaken for the model's bug. The testing node runs **related tests first** (`src/orchestrator/test_scope.py`: pytest modules matching the touched files' stems, jest `--findRelatedTests`, `go test` on the touched packages), then the full suite only once those pass — targeted failure output instead of a wall of unrelated tests. A new **`run_tests` tool** (`scope='related'|'all'`) gives the model the same runner mid-loop, and the system prompt now tells it to verify before signaling done instead of discouraging it. Found on the way: a Python repo identified only by `pytest.ini`/`tox.ini`/`conftest.py` was classified as an unknown ecosystem — fixed, with no install step for a repo that has nothing to build. All verified against a real sandbox: a real `pip install -r requirements.txt` succeeding and failing (the failure text attributed), and real pytest runs — related, full, "no related tests → full", and a genuine failure returned as information rather than a tool error.
- **Streamed coding turns** (`InferenceClient.stream_to_message`): every turn of the agentic loop now streams — tool calls and text are reassembled from OpenAI-style deltas into the same response shape `complete()` returns, with real token usage from the `stream_options.include_usage` final chunk (verified against a live llama.cpp server) and an `on_text` callback for live progress. Retries apply until the first chunk arrives; a stream that has started is never replayed. The fake vLLM fixture now serves real SSE (a tool call whose JSON arguments are split across chunks), and the frontier proxy emits a tools request as a well-formed chunk sequence instead of dropping the tool calls. `AGENT_STREAM_TURNS=0` restores blocking requests for a backend that cannot stream tool calls.
- **Summarise-on-overflow instead of drop-on-overflow** (`context.trim_turns_to_budget_async`): when the loop's conversation exceeds `AGENT_MAX_CONTEXT_TOKENS`, the oldest turns are condensed by the cheaper `coding_fallback` role into a "Context summary" the model keeps — files read and what was learned, edits applied, results seen — rather than a bare "N turns were dropped". Falls back to the plain notice if the summariser fails; a summary is never the reason a task fails.
- **Configurable quality-gate strictness**: `QUALITY_GATE_BLOCKING_TOOLS` (default `bandit`, `mypy`) and a per-task `quality_blocking_tools` on `AgentTaskRequest`, carried through the Temporal workflow input — list `ruff`/`eslint`/`tsc`/`go`/`cargo` to make lint findings send the task back to fixing. bandit's low-severity findings stay informational even when it blocks.
- **Guided fine-tune: describe → plan & cost → approve → train** (`keystone finetune guided`, `POST /v1/finetune/plans`, the web wizard's new step 1/2, `docs/guides/fine-tune-slm-on-your-repo.md`): the fine-tuning library's pieces — git-history and accepted-trajectory sources, the repository-stratified manifest, the trainers — existed and nothing wired them together; the only live path was bring-your-own JSONL with a 32B default. Now an admin names repositories and a goal; the server clones them, builds task → diff examples from real commits plus this tenant's accepted agent tasks, drops records carrying credentials (key-shaped content, private-key blocks — a redacted secret is still a leak of its shape) and redacts PII in the rest, splits by repository, writes a manifest with content hashes, picks the model from a new **SLM catalog** (`src/inference/catalog.py`, Qwen2.5-Coder 0.5B–32B with VRAM footprints; `auto` = the 7B coder stepped down to what fits) and the method from the **detected or stated GPUs** (`src/hardware.py`), counts optimizer steps exactly, labels time/cost estimates with their basis (`src/finetuning/planner.py`), and saves a `planned` job. Approval flips it to `pending` and starts the same runner every job uses; a plan that does not fit is refused unless forced. Trainer and API defaults moved from the 32B to the catalog's 7B. Verified against a real git repository and a real Postgres end to end, including the credential drop and the PII redaction.
- **Best-of-N coding** (`src/orchestrator/best_of_n.py`; `AGENT_BEST_OF_N`, `AgentTaskRequest.best_of_n`, the MCP `task_submit` tool; default 1 = off, no ceiling): N independent attempts of the coding loop from the same clean base commit, each on its own git branch inside the task's sandbox, each scored by the repository's own lint/typecheck commands and the tests related to what it touched — the same gates the task will face, never a model's opinion — and the winner's change applied back onto the working branch as ordinary uncommitted edits, so quality, review, tests, commit and PR are unchanged. Every candidate's score and the final ranking are `candidate` step events in the trace; losing branches are deleted. Verified in a real sandbox with a real git repository (`tests/test_best_of_n.py`): three scripted attempts — one that breaks pytest, one that passes but ruff flags, one clean — really scored by pytest and ruff, the clean one wins and lands, the branches are gone.
- **Hybrid retrieval and structure-aware chunking** (`src/memory/hybrid_search.py`, `src/memory/chunking.py`, migration `b3c7d9e1f2a4`): the agent's retrieval was vector-only, so the exact identifier a task names (`_refill`, `TokenBucket`) could lose to a topically similar chunk. Every indexed chunk is now also stored in Postgres with a generated tsvector and a GIN index (`code_chunks`, same id as its Qdrant point, written and deleted by the ingestion pipeline alongside the point), and task retrieval fuses the vector ranking with Postgres full-text ranking by Reciprocal Rank Fusion — a chunk found by both ranks first, a source that fails degrades to the other rather than to nothing. Memory recall ranks by Postgres full-text rank (stemming, phrases) before keyword overlap. Chunks cut on the code's own boundaries: Python via the real `ast` (a function or class with its decorators is one unit, small units folded, oversized ones split without loss), Go/JS/TS/Rust/Java/C#/Ruby/PHP on their definition keywords, everything else on the previous line window. Verified against real Qdrant and Postgres (`tests/test_hybrid_search.py`: an exact identifier found by full text, fused first when both sides agree, repository scoping on both sides; recall ranking with stemming) and `tests/test_chunking.py`.
- **`keystone doctor` knows more, and the references cannot drift**: doctor now also checks the database's Alembic revision against the code's head (an upgrade that forgot `alembic upgrade head` is caught before a request hits a missing column), the TLS certificate's validity and days left (a real PEM parse), free disk where adapters and datasets are written, and — with `KEYSTONE_INFERENCE_URL` set — the running app's own `/health/ready`. `docs/reference/cli.md` (every command's `--help`) and `docs/reference/api.md` (every route from the OpenAPI document) are generated by `scripts/gen_cli_reference.py`, and CI fails when either is stale. `docs/troubleshooting.md` is keyed to the exact messages the platform prints, each with what it means and what to do. Verified: `tests/test_doctor_checks.py` (a real self-signed certificate from openssl, the real test database's revision, real disk usage) and `tests/test_docs_gates.py`.
- **Production posture in the Helm chart** (`helm/keystone/templates/openbao-secretproviderclass.yaml`, `migrate-job.yaml`, `postgres-backup.yaml`, `networkpolicies.yaml`, `app.yaml`; `scripts/restore_drill.sh`; `benchmarks/load/k6_chat.js` + `scripts/k6_smoke.sh`): the OpenBao path `values-client-vpc.yaml` had enabled for a year referenced a template that did not exist — it exists now: a Secrets Store CSI `SecretProviderClass` that reads the six platform secrets from OpenBao and syncs them into the Secret every template already uses, mounted by the app and worker pods. Migrations moved from an initContainer racing on every replica to a `pre-install,pre-upgrade` hook Job that waits for Postgres and fails the release on error; the app Deployment rolls with `maxUnavailable: 0`, a preStop pause and a 60 s grace period. Nightly `pg_dump` backups to their own PVC (`postgres.backup`), pruned by retention, with a **restore drill** that restores a dump into a throwaway Postgres and compares the Alembic revision and row counts — run for real against the development database (every table matched) and on every CI push. Default-deny egress with explicit allows (`networkPolicy.egress`: DNS, in-cluster peers, operator-listed CIDRs, vLLM internet only when asked). New alerts: p99 latency, time to first token, upstream-error burn, budget rejections, PVC near full. A k6 load test for the gateway with real token usage and routing headers, run as a smoke in CI against the CPU demo model (`docs/benchmarks/load-test.md` records the smoke and how to produce GPU numbers). Everything renders in CI with every option enabled; the CSI sync and the hook ordering await a real cluster with the driver installed, stated in the runbook.
- **Gateway hardening** (`src/api/middleware/auth.py`, `src/api/middleware/request_context.py`, `src/api/routes/_inference_common.py`, `src/api/routes/keys.py`): API keys are now stored as HMAC-SHA256 keyed with the deployment's `VS_SECRET_KEY` (ADR 0004) — a copy of the database alone cannot verify a key; keys stored before the change (plain SHA-256) still authenticate and are re-hashed on their first use, so the legacy window closes by itself. **Breaking for production**: the app refuses to start with `VS_ENV=production` and no `VS_SECRET_KEY` (a per-process random pepper would invalidate every key at the next restart); `keystone doctor` reports it everywhere else. **Key rotation without an outage**: `POST /v1/admin/tenants/{id}/keys/{prefix}/rotate` / `keystone keys-rotate` mints a replacement with the same scopes, limits and user and gives the old key a 24-hour grace expiry (`grace_hours`, 0 = revoke now), audited with both prefixes. **A monthly dollar budget per tenant** (`Tenant.monthly_budget_usd`, migration `9d4f6a1c2b7e`, 0 = none; `POST /v1/admin/tenants/{id}/limits` / `keystone tenants set-limits` also change token budgets and concurrency after creation): once the priced ledger reaches it, gateway requests and new agent tasks answer 429 with `X-VS-Budget-Spent-USD` / `X-VS-Budget-Limit-USD`. **Every response carries `X-Request-ID`** (echoed or generated) and every log line for that request carries it plus the tenant and key prefix (structlog context variables); gateway responses carry **`X-VS-Route-Decision`** (`requested`, `served`, `model`, `reason=primary|fallback|adapter|cost_routing`) so a fallback or an adapter is never silent. `GATEWAY_COST_ROUTING=1` routes a simple `model="auto"` request to `coding_fallback` (off by default; a wrong downgrade costs quality). New metrics: time to first token per role, upstream errors per role, routing decisions, budget rejections. `LOG_PROMPTS=1` logs prompts and replies through PII redaction (off by default). Verified for real over the real application with real Postgres/Redis and a real backend process (`tests/test_gateway_hardening.py`: legacy key re-hashed, rotation and grace, budget refusing gateway and agent then lifted, request ids and route decisions, metrics present, cost routing) and `tests/test_auth.py` (peppered hash differs from the legacy hash and changes with the pepper).
- **Executive-level README and client docs**: the README now leads with what Keystone is, why teams choose it, what you get, how to get running at each tier, how it works, what it needs, and what is proven versus not — in plain language, with the implementation narratives moved into guides: `docs/guides/gateway-api.md` (call it from any OpenAI or Anthropic SDK, roles, changing models, scaling, adapters, retrieval, keys and spend), `docs/guides/coding-agent.md` (what "done" means, submitting tasks, sandboxes, the git server, safety on untrusted content, memory), `docs/benchmarks/README.md` (the two harnesses, the nine repository tasks, the real results so far and what is not published yet) and `docs/enterprise/open-weights-risk-and-support.md` (the sign-off page: risks, mitigations, who supports what, the questions to ask). `SUPPORT.md` states plainly how support works. Stale statements the old README carried — that the guided fine-tune path and the verdict gate were not built, that only one benchmark task existed — are gone.
- **Docs that cannot drift** (`mkdocs.yml`, `docs/getting-started/quickstart.md`, `docs/getting-started/hardware-sizing.md`, `docs/reference/configuration.md`, `docs/claims.yaml`): a documentation site built with `mkdocs build --strict` in CI; **one** quickstart with two tracks (no GPU: the llama.cpp demo model for the gateway and the frontier proxy for the coding task, labelled not air-gapped; one 24 GB GPU serving Qwen2.5-Coder-7B), whose steps 1 and 3 are executed literally by `tests/e2e/test_golden_path.py` against the real demo model; a hardware-sizing table per model with the formulas behind it; a configuration reference **generated from `src/config.py`** (every setting now carries its description in the source; `scripts/gen_config_reference.py --check` fails CI when the page is stale); and a truth-in-docs gate — every claim in `docs/claims.yaml` must appear verbatim in the document it is attributed to and name a test pytest actually collects (`scripts/check_claims.py`; its own failure modes are tested). `keystone init --backend demo-cpu|single-gpu|frontier-proxy|self-hosted-gpu` writes the matching `.env` (the compose `vllm-coding` service now reads `VLLM_CODING_MODEL/_GPU_COUNT/_MAX_MODEL_LEN/_TOOL_PARSER`, so serving a 7B on one GPU is a `.env` choice, not a compose edit) and `keystone up --with-demo-model` starts the no-GPU backend.
- **Model Library + Playground** (`GET /v1/keystone/models`, `src/inference/library.py`, web views **Models** and **Playground**, `docs/guides/model-library-and-playground.md`): one answer to "what models can I use here, and are they up?". Serving roles carry their **live state** from the router's own cached health registry — the same probe and breaker the next request will hit, not a second opinion — with the endpoint's real `/models` listing (served ids, `max_model_len` when the server reports it); this tenant's adapters follow their base role's state and show verdict, rank and path; the catalog lists deployable, fine-tunable models with the VRAM to serve and to train, `TRAINING` while a job on them runs. Each entry states the API features a client can use through the gateway, with `tools` left `null` for a model outside the catalog rather than assumed. The UI's cards copy the exact `model` id, open a READY model in the Playground, prefill the guided fine-tune wizard, or print the exact Helm `--set` / `keystone deploy runpod-serverless` lines. The Playground is a streamed `/v1/chat/completions` request — nothing special-cased for the UI — showing the backend's real token usage, time to first token, tokens/s, the role that actually served (a fallback is visible) and the request as curl. Verified for real: `tests/test_model_library.py` runs the route against a real backend process and dead ports with a promoted adapter, a retired one and a running fine-tune in a real Postgres.
- **Anthropic-compatible Messages API** (`POST /v1/messages`, `POST /v1/messages/count_tokens`; `src/api/routes/messages.py`, `src/inference/anthropic_compat.py`): a client written for the Anthropic Messages API — its SDKs, agent harnesses and IDE tools — now talks to the self-hosted open-weight models (or a tenant's promoted adapter) by changing its base URL. The request is translated to the one wire protocol every backend here speaks (OpenAI chat completions) and the reply back: text, tools (`tool_use` out, `tool_result` in, streamed tool arguments as `input_json_delta`), the full stream event sequence (`message_start` … `message_stop`) with the backend's real token usage in `message_delta`, and Anthropic's error envelope for what is not translated (images, documents, server tools) — refused, never dropped. The same admission path as the OpenAI route (`src/api/routes/_inference_common.py`, now shared so the two cannot drift): deployment ceiling, per-key rate limit, tenant budget, health-aware routing with 503 + Retry-After, adapter resolution, token accounting and the dollar ledger. The translation the frontier proxy already used (`benchmarks/frontier_proxy.py`) moved into the same module, so both directions live in one tested place. **`/v1/chat/completions` now passes tool calling through**: `tools`, `tool_choice`, `response_format`, assistant `tool_calls` and `tool` messages reach the backend unchanged — an OpenAI client in agent mode (Continue, OpenCode) gets the full round trip; `tools` with `response_format` in one request is refused with 422 as vLLM cannot serve both. Verified for real: every shape over the real route with the real `InferenceClient` talking HTTP to a real backend process (`tests/test_messages_api.py`, `tests/test_anthropic_compat.py`), and end to end against the live llama.cpp demo model in CI (`tests/e2e/test_messages_api_ci_backend.py`).
- **Verdict gate, served in seconds, live training progress** (`src/finetuning/verdict.py`, `src/inference/lora_registry.py`, `docs/guides/fine-tune-slm-on-your-repo.md` § 3): promotion was a bare status check — any completed job could become the tenant's default adapter, and a promoted adapter reached vLLM only if someone hand-edited `--lora-modules` and restarted. Now the trainer evaluates the untouched base model on the held-out split *before* training and the adapter after, on identical data; the job's metrics carry both and a **verdict** (`pass` when the adapter beats the base by ≥1 %, `fail` when it does not, `unknown` when there is no held-out comparison). `POST /v1/finetune/jobs/{id}/promote` refuses a `fail`/`unknown` verdict with 409 and the reason; only an admin can `?force=true`, and the force is written to the audit log with the verdict it overrode. A promotion then rewrites `<FINETUNING_OUTPUT_DIR>/lora_modules.json` (every promoted adapter per base model plus the ready `--lora-modules` argv the serving launch reads on restart — Helm `vllm.<role>.lora.{enabled,maxRank,maxLoras,modules}` and the compose `VLLM_CODING_EXTRA_ARGS` consume it) and calls vLLM's `POST /v1/load_lora_adapter` on the role serving that base model, so the adapter is live without a restart when `VLLM_ALLOW_RUNTIME_LORA_UPDATING=True` (set by the chart and compose). A server that refuses, or a manifest directory that is not mounted, is reported in the promote response's `metrics.serving` and the log, never turned into a 500 after the promotion committed. Training progress is a real transformers callback: every logged step (loss, held-out loss, step/total, elapsed, ETA, tokens/s) is published as a fine-tune event, streamed by `keystone finetune watch` and plotted in the web UI, which also shows the verdict and the force control for admins. Verified for real: the promote gate end to end over the real route against a real Postgres (refused, non-admin force refused, admin force audited, pass promoted, manifest written, live load attempted), and the base eval plus per-step progress in the CPU trainer run (`tests/test_trainer_smoke.py`).
- **Repo maintainability**: `uv.lock` (verified: `uv sync --frozen --extra dev` reproduces the dev environment in a clean venv; CI checks the lock stays in sync with `pyproject.toml`), a `.pre-commit-config.yaml` running exactly CI's lint job (ruff, ruff-format, mypy, the vendor-neutral text check), `scripts/check_vendor_neutral.sh` enforcing ADR 0006 over README/CHANGELOG/ROADMAP/docs in CI, and the architecture decision records the code and plan cite — `docs/architecture/adr/` 0001–0006 with an index.
- **VS Code via Continue** (`keystone ide continue-config`, `docs/guides/use-from-vscode.md`, `vscode/continue/config.example.yaml`): one generated `config.yaml` points Continue's chat/edit/apply/summarize at the `coding` role, autocomplete at `coding_fallback`, and Continue's MCP client at Keystone's memory tools over streamable-HTTP with bearer auth — the same gateway and key OpenCode uses, with an optional internal CA bundle for air-gapped TLS. Model names are gateway roles, so swapping the model behind a role (a fine-tuned adapter, the frontier proxy) needs no IDE change. Every field name was checked against Continue's own config-yaml zod schema, and `tests/test_vscode_continue_config.py` parses the generated file and re-checks them, so a wrong field (which Continue would silently ignore) fails CI.
- **A first-party VS Code extension** (`vscode/extension/`, publisher `keystone`, `docs/guides/use-from-vscode.md` § "The Keystone extension"): the background agent from inside VS Code. `Keystone: Submit background task` — description, repository pre-filled from the workspace's git `origin`, and a model picker built from the live `GET /v1/keystone/models` (READY roles first, an UNHEALTHY / NOT_DEPLOYED / TRAINING role shown with its state and not selectable, `auto` always available) — then a trace panel streaming `GET /v1/keystone/tasks/{id}/stream` through the same fetch-based Bearer-authenticated SSE reader the web UI uses: phases, plan, per-node stats and every step (tool calls and results, model messages, dependency install, repository map, routing decision, quality findings, test output, diff, PR) with full bodies, and an **Open PR** notification when the engine's final event arrives. `Open task trace`, `List recent tasks` (quick pick → trace), `Search team memory` (`POST /v1/keystone/memory/recall`), `Open the Playground`, `Set API key` (VS Code secret storage, never `settings.json`), and a status bar item with the tenant's pending/running count. Verified: `npm test` runs the SSE decoder, renderer and picker against **recorded real frames** — `src/__tests__/fixtures/task-stream.sse` is the byte-exact output of the real stream route captured through the FastAPI app over a real Redis Stream, `model-library.json` the real library route with one role served by a real backend process (`scripts/record_task_stream_fixture.py`; not a recording of a live model writing code — the payload shapes are those each publishing call site emits); `npm run test:integration` runs a real VS Code (`@vscode/test-electron`) that activates the extension and checks every command, and — only when `KEYSTONE_URL`/`KEYSTONE_API_KEY` are set — submits a real task and asserts the panel receives real events, otherwise it says so and skips. CI's new `vscode-extension` job runs all of it and uploads `keystone-agents.vsix`. `scripts/check_ts_types_in_sync.py` (CI lint job, `tests/test_ts_types_in_sync.py`) keeps the `NodeEvent`/`StepEventType`/`StepEvent`/`TaskEvent` declarations identical between `web/src/api.ts` and `vscode/extension/src/api.ts`.
- **MCP `task_submit` and `task_status` tools** (`src/api/routes/mcp.py`): any MCP client (Continue, OpenCode, a desktop client) can now submit a background task and read its status next to the memory tools. `task_submit` builds the very same `AgentTaskRequest` and calls the very same `submit_agent_task` function the REST route does (extracted from `POST /v1/keystone/tasks`, not copied), so validation — task length, the role literal, `max_iterations ≥ 1` — and the concurrency check are one implementation; `task_status` returns the `GET /v1/keystone/tasks` summary shape for one task, scoped to the calling tenant (another tenant's id is "not found"). Verified in `tests/test_mcp_server.py` over the real streamable-HTTP transport against a real Postgres: a task submitted over MCP shows up in the REST list, the three validation refusals carry readable field messages, tenant scoping, a bad UUID, and the fail-closed cases (bogus key, missing header) for both tools.
- **Cached endpoint health, a per-role circuit breaker, and honest 503s** (`src/inference/health.py`): the router probed `GET /models` on the model endpoint for every request and, when nothing answered, handed back the unhealthy client anyway — a probe per request against a remote (RunPod) endpoint, and a scale-to-zero worker that is cold after idle would fail every probe and still be "served". A probe result is now reused for `INFERENCE_HEALTH_CACHE_SECONDS` (10 s); `INFERENCE_BREAKER_FAILURE_THRESHOLD` (3) consecutive failures — failed probes or errored requests — open a role's breaker for `INFERENCE_BREAKER_OPEN_SECONDS` (30 s), during which the fallback chain skips it without a probe and lets one request through when the cooldown ends. With nothing healthy in the chain the gateway answers **503 with `Retry-After`** instead of a downstream failure. `/health/live` (process up) and `/health/ready` (Postgres + Redis, 503 otherwise; model roles reported but never probed there, so readiness never blocks on a model) replace the single `/health` in the Helm probes — a liveness probe that depended on the database would restart every pod during a database blip. `/health` keeps the full picture, including each role's breaker state.
- **Nine repo-task benchmarks, each proven honest before use**: eight new Python tasks join `token_bucket` (`lru_cache_recency`, `csv_quoted_fields`, `slugify_collapse`, `backoff_cap`, `date_range_inclusive`, `inventory_negative_stock`, `pagination_last_page`, `config_deep_merge`) — each a small real repository with a real bug, a held-out `fail_to_pass` test that fails on the unfixed code and `pass_to_pass` tests that must keep passing. `tests/test_repo_tasks_well_formed.py` runs every task's own pytest against a scratch copy of its repo and asserts exactly that, so a task whose premise is wrong cannot enter the suite — it immediately caught one (a "must-fail" test that already passed on the buggy code). Benchmark fixture repos are excluded from the project's own linter, since one of their findings would be the deliberate bug. The seeder no longer copies local tool caches into the git host.
- **Persisted, reproducible benchmark evidence** (`benchmark_runs`, migration `8c3e5f0a2b41`): `benchmarks/agent_runner.py --persist --backend-label <label>` writes every repo-task result — held-out `fail_to_pass`/`pass_to_pass` outcomes, tokens, wall time, PR, the full runner record — as a row tagged with the backend it ran against; `benchmarks/compare.py --backend LABEL=URL[:MODEL]` sweeps the same harness across several backends (open-weight, frontier proxy, a fine-tuned adapter) and `benchmarks/report.py` regenerates `docs/benchmarks/latest.md` from the rows — solve rate per backend over the latest run of each task, tokens, wall time, cost where a price is configured, and a per-task grid — so the published table can never say something the database doesn't. `model_adapters.benchmark_run_id` finally became a real foreign key (SET NULL on delete), verified against a real Postgres.
- **A durable usage ledger with dollars** (`src/billing/ledger.py`): `usage_records` existed as a table nothing wrote to — token accounting lived only in expiring Redis counters. Every gateway completion (both paths) and every agent task (per model role, from its execution trace) now lands in one row per tenant, API key, model role and hour, upserted atomically on a `NULLS NOT DISTINCT` unique index (migration `7b2d4e8f1a90`) so concurrent requests never lose an increment. Dollars come from `MODEL_PRICES_PER_MILLION` — USD per 1M tokens per role, set from your own GPU economics (`benchmarks/cost_model.py` is the calculator); an unpriced role is recorded with cost 0 and the API says pricing is not configured rather than inventing a number. `GET /v1/keystone/usage?days=30` (a tenant's own spend, per day and role, with totals) and `GET /v1/admin/tenants/{id}/usage`; the web UI gained a **Spend** view. Verified against a real Postgres: 25 concurrent writes summed exactly in one bucket; a real completion through the gateway shows up in the usage route with the configured price applied.
- **Streaming requests are billed for real**: the gateway's streaming path counted ~1 token per SSE chunk and never counted the prompt. It now always asks the backend for `stream_options.include_usage` (vLLM and llama.cpp both honor it), records the backend's real usage from the final chunk (forwarding that chunk to the caller only when the caller asked for it, as an OpenAI client that did not may not expect a chunk without choices), and — if a backend sends no usage at all — counts prompt and completion tokens with the agent's own tokenizer and logs that it is an estimate. Both paths now label usage metrics with the model role.
- **A real live trace** (transparency): the task event stream carried one summary per graph node; it now also carries every step *inside* a node as it happens — `tool_call`, `tool_result` (the whole output), `model_text`, `test_output` (the whole stdout/stderr), `quality_findings`, `deps_install`, `repo_map`, `route_decision`, `diff`, `pr` — with full payloads and no stream trimming (the previous `MAXLEN 500` silently dropped a long task's earliest steps). The same steps are persisted on each `IterationRecord` in the task's execution trace, so the record in Postgres is as complete as the live feed. The stream now closes on the engine's `final` event, published after the commit/push/PR rather than at the graph's terminal phase, so a viewer sees the diff and the pull request before the connection ends. The web trace view gained the quality phase and a "Live steps" log with expandable bodies (tool output, test output, diff) and shows the PR link and commit on completion. Verified against a real Redis: 1,200 steps read back complete and in order, a 250 kB tool output intact.
- **No policy limits and no lossy truncation anywhere** (production posture): every budget is now 0 = unlimited by default — tenant daily/monthly tokens (were 5M/100M), the gateway's per-key request rate (was 60/min), `MAX_TOKENS_PER_REQUEST` (was 32,768), the per-task token cap (was 2M), the whole-task wall clock (was 30 min) — and `max_iterations` lost its ceiling of 50 (it remains the one per-task *safety* bound, set per task). Timeouts for the repo's tests, dependency install, quality tools and `run_command` are now generous, configurable settings instead of 180–600 s constants. Every fixed-size slice on persisted data is gone: test output and errors, quality findings, the task diff (was cut at 200 kB), install logs, git-host/sandbox error messages and trace records are kept in full; the commit message and PR body carry the complete task description (only git's 72-character subject convention and Gitea's 255-character title limit apply, both external). The single remaining truncation is fitting text into one model prompt — `context.fit_to_tokens`, budgeted from `AGENT_MAX_CONTEXT_TOKENS` rather than any fixed byte count, keeping the tail of test output (where the failures are) and the head of files/diffs, and stating exactly how much was omitted and where the full text lives.
- **No concurrency cap by default** (multiple developers, multiple repos, in parallel): `Tenant.max_concurrent_agents` defaulted to 3 with no API to change it, so every tenant's fourth concurrent task was rejected with HTTP 429 — a policy number standing in for capacity. The column now means 0 = no cap (the new default; migration `6a1c2f9e0b3d` moves rows still holding the old default of 3 to 0), the sandbox daemon's per-tenant cap follows the same rule (`SANDBOX_MAX_CONCURRENT=0`), and a positive cap is a deliberate admin choice via `POST /v1/admin/tenants` `max_concurrent_agents`, still enforced with per-user fairness when set. Each task already runs in its own sandbox, own clone, own branch and own PR, so nothing serialises on a repository; throughput is bounded by the worker, sandbox and GPU capacity you deploy (Temporal workers and sandbox daemons scale horizontally; vLLM autoscaling is KEDA-driven).
- **Static type checking in CI** (`mypy src/`, zero errors, enforced in the lint job): `src/db/models.py` converted from classic `Column(...)` declarations to SQLAlchemy 2.0 `Mapped[...]`/`mapped_column(...)` so every ORM attribute carries its real Python type — that one change took the baseline from 236 errors to 102, and the rest were fixed at the source rather than baselined: nullable columns handed to non-optional response fields, `Optional` values passed unchecked into git-host and sandbox calls, a budget comparison against a possibly-unset limit, and a graph builder annotated as returning the uncompiled graph. Nullability was preserved exactly through the conversion (`alembic check` reports no drift), then made honest where the code already assumed a value: migration `519d4719f677` backfills and declares `created_at` on every table, `api_keys.scopes`, `finetune_jobs.status`, and `codebase_indexes.chunk_count` NOT NULL. Verified for real: a row inserted outside the ORM with a NULL `created_at` was backfilled by the migration, the constraint held, and downgrade/upgrade round-tripped clean; the full suite (438 tests) passed against the migrated schema.
- **GPU pilots on AWS, Azure and Google Cloud** (`infra/opentofu/modules/{aws-eks-gpu,azure-aks-gpu,gcp-gke-gpu}`, `infra/opentofu/environments/{aws,azure,gcp}-pilot`, `helm/keystone/values-{aws,azure,gcp}.yaml`, `keystone deploy cloud`, `docs/guides/deploy-{aws,azure,gcp}.md`, `docs/deployment/verification-log.md`, CI job `iac-validate`): one OpenTofu module per cloud creates the managed Kubernetes cluster (EKS 1.33 / AKS with Azure CNI overlay and Cilium / GKE Standard with Dataplane V2), a CPU system pool, one tainted 24 GB GPU node pool (g6.2xlarge L4 / Standard_NV36ads_A10_v5 A10 / g2-standard-8 L4, min 0 so it can scale away), the block and ReadWriteMany CSI drivers, an RWX StorageClass for the shared model cache (EFS access points / Azure Files Premium NFS / Filestore) and a block StorageClass (gp3 / Premium SSD / pd-balanced), and — on AWS and Azure, whose images ship the driver but not the plugin — NVIDIA's k8s-device-plugin; GKE installs its own. Each module outputs the cluster endpoint, the kubeconfig command, both StorageClass names and the GPU nodeSelector/toleration the chart needs, and each `values-<cloud>.yaml` overlay, stacked on `values-client-vpc.yaml`, fills every placeholder that file left open (storage classes, node labels, `sandboxDaemon.backend: gvisor`, a single-GPU Qwen2.5-Coder-7B coding role). `keystone deploy cloud --cloud aws|azure|gcp --env pilot --plan|--apply|--destroy [--helm-install]` streams `tofu -chdir=<env> init` plus the action (Terraform when OpenTofu is absent; a clear refusal when neither is installed; `apply`/`destroy` keep their interactive confirmation), then prints or runs the matching `helm upgrade --install`; the argv builders live in `src/cli/ops.py` and are asserted exactly in `tests/test_cli_ops.py`, which also runs a real `init -backend=false` + `validate` through them. Each guide states what you get, the list-price cost per hour with the date it was written, the five commands, and what is verified. **Honest scope: `tofu fmt`, `tofu init -backend=false`, `tofu validate` (aws 6.66 / azurerm 4.81 / google 6.50 / kubernetes 2.38 / helm 2.17) and `helm lint`/`helm template` with each overlay pass locally and in CI; no `apply` has been run on any cloud account, so nothing here claims a cluster, a scheduled GPU, or a served model — `docs/deployment/verification-log.md` says so and gives the template for recording the first real apply.**

### Fixed
- **The live task stream closed at the model's last coding message instead of at the end of the task.** `is_final_event` (`src/orchestrator/events.py`) treated any event carrying `final: true` as the engine's closing event — but the coding loop's `model_text` step also carries `final: true` (its "last message of the turn, no tool calls" flag, `nodes/coding.py`), so `GET /v1/keystone/tasks/{id}/stream` ended right after the model finished editing, before the quality gates, review, tests, diff and pull request were ever sent to the viewer. Found by recording the real stream for the VS Code extension's fixture: 19 events were published, 9 arrived. Only a node event's `final` closes the stream now; `tests/test_events_steps.py` publishes a `final: true` model message mid-stream against a real Redis and asserts the stream continues to the engine's event.
- **The sandbox's egress firewall never actually allowed a deployment's real configured `GIT_ALLOWED_HOSTS`** — only the hardcoded placeholder `*.internal.keystone.local` hostnames, regardless of what an operator set. A custom git host correctly passed the SSRF allowlist check (`_validate_repo_url`) and then had its real `git clone` silently blocked by egress DENY, completely breaking the README's own "Connect your own git server" instructions for anyone who followed them. Found and fixed by actually running `tests/test_git_workflow_integration.py` end to end against a live Gitea for the first time — `build_egress_policy()` now wires the real configured git/mirror hosts into real ALLOW rules.
- **`docker-compose.yml`'s sandbox network had no pinned name**, so Compose silently prefixed it with the project name; every network-enabled sandbox (any git operation) then failed outright with a real 404 from the Docker API. Same discovery pass as above.
- **`tests/test_cli_doctor.py` hardcoded this dev machine's own local port numbers** instead of reading `DATABASE_URL`/`REDIS_URL`/`QDRANT_HOST`/`QDRANT_PORT` from the real ambient environment like every other integration test — passed locally by coincidence, broke `main`'s real CI for several commits (different service-container ports there) before a direct check of the GitHub check-runs API — not just `gh run watch`'s reported exit code, which had been giving false "success" signals for runs that were actually being cancelled by rapid sequential pushes — caught and fixed it.
- **The `/v1/keystone/ingest` route never caught `InvalidRepositoryURLError` or a failed `git clone`**, so a disallowed host or a nonexistent branch surfaced to the caller as an opaque 500 instead of the real reason — found by actually running `keystone ingest` against a disallowed host and a bad branch, not just the happy path. Now mapped to a clean 400 with the actual error message.
- **The circuit breaker had no limit on consecutive quality-gate failures** — the quality node could route CODING → QUALITY → FIXING indefinitely, bounded only by the iteration and token budgets. `CircuitBreakerConfig.max_consecutive_quality_failures` (default 3) now trips it like the test and review limits do.
- **`MAX_TOKENS_PER_REQUEST` was a dead setting** — defined in `src/config.py`, referenced nowhere. `/v1/chat/completions` now rejects a request above the deployment's ceiling with a 422 before routing, instead of passing anything up to the protocol maximum through to the model.
- **The fake vLLM test fixture could error at setup on a slow CI runner** — its readiness probe caught only `httpx.ConnectError`, so a `ConnectTimeout` during uvicorn's bind escaped as a fixture error rather than a retry (seen for real in CI). It now retries on any `httpx.TransportError`.
- **Removed dead code**: the batch inference queue (`src/inference/batch.py`, `batch_worker.py`) had no HTTP route and no worker deployment; `AgentState.cumulative_diff`, `max_review_retries`, and `max_test_retries` were never read (the real limits live in `CircuitBreakerConfig`).
- **All three trainers passed `warmup_ratio` to `TrainingArguments`, which transformers 5.x removed** — a `TypeError` on the first real run, invisible for as long as no test executed a trainer; `pyproject.toml` pins `transformers>=4.44` unbounded, so CI would have hit it too. `src/finetuning/_common.py`'s `warmup_kwargs` now resolves the right keyword from the installed class's real signature: `warmup_ratio` on 4.x, `warmup_steps` as a float ratio on 5.x (identical semantics), rather than pinning the ecosystem back.
- **The LoRA trainer forced model weights to float16 even with `fp16=False`** — the dtype is now tied to the requested mixed-precision mode, so a CPU run (neither fp16 nor bf16) loads float32 instead of failing.
- **transformers 5.x's threaded weight loader segfaults on macOS arm64** (`core_model_loading._materialize_copy`, torch 2.14 / transformers 5.17, observed with a real fault stack) before any training code runs. The smoke test sets transformers' own `HF_DEACTIVATE_ASYNC_LOAD` switch; the trainer itself is untouched.
- **Documentation now says only what the code does**: the README's `"status": "ok"` health example (the code emits `"healthy"`), an unbacked claim that a CPU LoRA smoke run had proven the trainer path (at the time, no automated test had ever executed a trainer — one now does, see Added), a stale `model_router.py` docstring saying adapter routing wasn't wired into the orchestrator nodes (it is), and a SUPPORT.md line written before `keystone doctor` shipped. Vendor model names are no longer used in repo-facing text; cost reports use vendor-neutral tier labels.

### Known limitations (tracked, not hidden)
- Model-quality behavior on Keystone's own self-hosted models (the actual LLM output) is unverified in this codebase's development environment, which has no GPU — see `docs/deployment/KUBERNETES_CLIENT_VPC.md` for what's verified vs. GPU-gated.
- No adapter has been fine-tuned on real GPU hardware yet, and fine-tuning's "only promote if it beats base+RAG" auto-verdict gate isn't wired up — promotion is a manual decision today.
- The full multi-file-repo benchmark suite has only one real task (`token_bucket`) — the phase calls for 10-20, plus a persisted comparison sweep (base / base+RAG / fine-tuned / frontier) and a web UI panel, none of which exist yet.
- See `ROADMAP.md` for the full phase-by-phase status, including what's checked and what each checked phase's known gap still is.

[Unreleased]: https://github.com/GGChamp85/keystone/commits/main
